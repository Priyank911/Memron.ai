// ═══════════════════════════════════════════════════════════════════
// @memron/lit-encryption — Identity-based threshold decryption via Lit Protocol
// Ensures only authorized DIDs can decrypt memory content.
// ═══════════════════════════════════════════════════════════════════

export { LitEncryptionService } from './encryption-service';
export { AccessControlBuilder } from './access-control-builder';
export { SessionManager } from './session-manager';
export type { EncryptionResult, DecryptionResult, LitConfig } from './types';
